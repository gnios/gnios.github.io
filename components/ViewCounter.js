export default function ViewCounter({ slug, className, blogPage = false }) {
  // Componente simplificado para GitHub Pages - sem contagem de visualizações
  return <span className={className}>–––</span>
}
